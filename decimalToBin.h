#include <string.h>
#include <stdio.h> 
#include <string.h> 
#include <stdlib.h>
#include <math.h>

int binary_to_decimal(int ,int ,int );
char  number_to_special4(int ,int );
int * number_to_bit(int ,int );
int * char_to_bit(char ,int );